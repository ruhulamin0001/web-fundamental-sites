#!/usr/bin/env bash
# DEPRECATED — replaced by the Docker + GitHub deploy flow. See ../README.md.
# The VPS now runs a single Caddy container built from ../Dockerfile.
echo "Deprecated. Use Docker deploy — see README.md" >&2
exit 1
